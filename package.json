{
  "name": "drift",
  "version": "1.0.0",
  "private": true,
  "description": "第一人称星际漫游 — WebGL / Three.js",
  "type": "module",
  "scripts": {
    "dev": "node serve.mjs",
    "start": "node serve.mjs",
    "test": "vitest run",
    "spec": "node scripts/generate-spec.mjs"
  },
  "devDependencies": {
    "vitest": "^3.0.5"
  }
}
