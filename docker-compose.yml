services:
  web:
    image: node:22-bookworm
    working_dir: /app
    volumes:
      - .:/app
    ports:
      - "5180:5180"
    command: sh -c "test -f vendor/three/build/three.module.js || npm install; node serve.mjs"
