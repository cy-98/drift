# 可选氛围音乐

将治愈向循环曲目命名为以下任一文件并放在此目录：

- `drift-theme.mp3` / `drift-theme.ogg`（主主题）
- `drift-theme-b.mp3` / `drift-theme-b.ogg`（可选第二首）
- `drift-theme-c.mp3` / `drift-theme-c.ogg`（可选第三首）

多首存在时会按播放结束自动轮播；仅一首时无缝循环。

建议：2–3 分钟无缝循环、无突兀打击乐、-14 LUFS 左右。
